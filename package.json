{
  "name": "nlp-treeparser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "mkdir -p dist && browserify nlp/sentenceToTree.js --standalone nlp-treeparser -o dist/index.js -t [babelify --presets [ es2015 ]]"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.12.4",
    "d3": "^3.5.5",
    "express": "^4.12.4",
    "nlp_compromise": "1.1.5",
    "retext": "^0.5.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0"
  }
}
