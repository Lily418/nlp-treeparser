{
  "name": "nlp-treeparser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "mkdir -p dist && node_modules/.bin/browserify nlp/sentenceToTree.js --standalone nlp-treeparser -o dist/sentenceToTreeBundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.12.4",
    "d3": "^3.5.5",
    "express": "^4.12.4",
    "nlp_compromise": "^2.0.1",
    "retext": "^0.5.1"
  },
  "devDependencies": {
    "browserify": "^12.0.1"
  }
}
